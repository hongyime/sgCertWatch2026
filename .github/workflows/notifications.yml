name: Retired Notification Outbox

on: workflow_dispatch

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: echo "Notification delivery is retired. Use the monitoring dashboard."
